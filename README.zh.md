# Skills Hub (Tauri 桌面版)

一个基于 Tauri + React 的跨平台桌面应用，用于统一管理 AI Agent Skills，并将其同步到多个 AI 编码工具的全局技能目录（优先使用符号链接/目录连接，回退到复制）——**"一次安装，处处同步"**。

感谢原作者的无私贡献,原始项目地址：
https://github.com/qufei1993/skills-hub
改动说明:
本项目在原始项目基础上进行了大量功能扩展与性能优化。
由于代码改动范围较大、冲突较多，目前尚未逐项提交审核
主要新增功能包括：- 支持自定义扫描路径；
- 支持 Skill 自定义目录、配置目录及展示说明；
- 点击 Skill 可查看详情，包括文件树预览、原始内容、翻译及 AI 优化建议；
- 提供 Skill 使用仪表盘，并支持通过 SDK 或 MCP 协议上报使用数据；
- 实现 Skill 的自动发现与一键安装；
- 内置常用 Skill，并集成增强版 Skill Creator 工具。
- 当前 AI 优化模块较为简略，因其核心能力正在另一个独立项目中深度打磨,本 Skill Hub 管理系统实为该主项目的子模块。
- 项目目标是构建完整的数据闭环与自进化能力，相关功能完善后将择机开源。

## 📖 文档

- **中文（本文档）**：`README.zh.md`
- English: [`README.md`](README.md)

设计文档：

- 系统设计（中文）：[`docs/system-design.zh.md`](docs/system-design.zh.md)
- System design (EN): [`docs/system-design.md`](docs/system-design.md)

## ✨ 核心功能

### 🎯 统一管理视图
- 集中展示所有托管的 Skills 及其在各工具中的激活状态
- 实时查看每个 Skill 的来源、更新时间和同步状态
- 支持按更新时间、名称排序，以及关键词搜索和分类筛选
- 支持自定义扫描路径和目录管理
  ![Skills Hub](docs/assets/set-path.png)
  ![Skills Hub](docs/assets/skill-add.png)
  ![Skills Hub](docs/assets/skill-category.png)

### 🔄 智能同步机制
- **混合同步策略**：优先使用符号链接（symlink）/目录连接（junction），在受限环境下自动回退到复制
- **多工具支持**：支持 40+ AI 编码工具，包括 Cursor、Claude Code、Codex、Windsurf 等
- **冲突检测**：自动检测目标目录冲突，提供明确的错误提示和解决方案

### 📦 多种导入方式
- **本地文件夹导入**：从本地目录导入 Skill
- **Git 仓库导入**：支持 GitHub URL，自动解析并克隆仓库
- **多 Skill 仓库选择**：智能识别包含多个 Skills 的仓库，提供候选选择界面
- **快速安装推荐**：内置推荐 Skill，一键安装


### 🔍 Skill 发现与浏览
- **发现市场**：浏览社区精选 Skills，支持分类筛选和搜索
- **GitHub 集成**：支持搜索 GitHub 仓库并直接安装
- **自动同步**：自动同步 awesome-claude-skills 数据库
  ![Skills Hub](docs/assets/skill-discovery-install.png)
  

### 📝 Skill 详情与编辑
- **文件浏览器**：查看 Skill 的完整文件结构
- **多模式编辑器**：
  - **编辑模式**：直接编辑 Skill 文件
  - **预览模式**：实时预览 Markdown 渲染效果
  - **分屏模式**：编辑和预览同时显示
  - **翻译模式**：一键翻译 Skill 内容（支持多种翻译服务）
  - **优化模式**：集成 AI 对话面板，智能优化 Skill
- **实时保存**：自动保存更改，支持手动触发保存
  ![Skills Hub](docs/assets/skill-content-ai.png)

### 📊 数据分析
- **调用追踪**：记录 Skill 的调用次数、成功率、耗时等指标
- **趋势分析**：可视化展示使用趋势和热门 Skills
- **成本分析**：统计 API 调用成本
- **用户留存**：分析用户行为和留存率
- **告警系统**：异常使用情况自动告警
  ![Skills Hub](docs/assets/skill-analyze.png)

### 🛠️ MCP Server 集成
内置 **MCP (Model Context Protocol) Server**，让 Claude AI 能够直接追踪 Skill 调用并查询分析数据，无需编写代码。

### ⚙️ 高级功能
- **自动更新**：支持 Git Skills 的自动更新检测
- **缓存管理**：智能清理 Git 缓存和临时文件
- **主题切换**：支持浅色、深色和系统主题
- **国际化**：支持中英文界面切换
- **翻译服务**：集成 Google 翻译、LibreTranslate 等翻译服务

## 🏗️ 系统架构

### 技术栈

#### 前端
- **框架**：React 19 + TypeScript
- **构建工具**：Vite 7
- **路由**：React Router DOM 7
- **UI 组件**：
  - Tailwind CSS 4.1
  - Lucide React（图标）
  - Motion（动画）
  - Sonner（通知）
  - Recharts（图表）
- **编辑器**：
  - EasyMDE（Markdown 编辑器）
  - React Markdown（渲染）
  - Streamdown（代码、数学公式、Mermaid 图表）
- **国际化**：i18next
- **状态管理**：React Context + Hooks

#### 后端
- **框架**：Tauri 2
- **语言**：Rust
- **数据库**：SQLite
- **核心模块**：
  - Tool Adapters（工具适配层）
  - Sync Engine（同步引擎）
  - Installer（安装器）
  - Git Fetcher（Git 拉取）
  - Analytics Store（分析存储）
  - Discovery Module（发现模块）

### 架构分层

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层 (React)                        │
├─────────────────────────────────────────────────────────────┤
│  Skills Dashboard  │  Skill Detail  │  Discovery  │  Analytics │
├─────────────────────────────────────────────────────────────┤
│                    Hooks & Context                           │
│  useSkillManagement  │  useToolManagement  │  useTranslation │
├─────────────────────────────────────────────────────────────┤
│                    UI Components                             │
│  FilterBar  │  SkillsList  │  Modals  │  FileTree  │  ChatPanel │
├─────────────────────────────────────────────────────────────┤
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│              Tauri IPC Layer (Commands)                      │
├─────────────────────────────────────────────────────────────┤
│  Skill Commands  │  Tool Commands  │  Analytics Commands    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    后端核心层 (Rust)                         │
├─────────────────────────────────────────────────────────────┤
│  Tool Adapters  │  Sync Engine  │  Installer  │  Git Fetcher │
│  Skill Store    │  Analytics    │  Discovery  │  Onboarding  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据存储层                                │
├─────────────────────────────────────────────────────────────┤
│  SQLite Database  │  File System  │  Git Cache  │  Temp Files │
└─────────────────────────────────────────────────────────────┘
```

### 代码入口

#### 前端入口
- **主入口**：`src/main.tsx`
- **应用根组件**：`src/App.tsx`
- **路由配置**：
  - `/`：主仪表盘（Skills 列表）
  - `/skill/:skillId`：Skill 详情页
  - `/skills`：重定向到 `/`

#### 后端入口
- **主入口**：`src-tauri/src/main.rs`
- **核心初始化**：`src-tauri/src/lib.rs`
- **命令定义**：`src-tauri/src/commands/mod.rs`
- **核心模块**：`src-tauri/src/core/`

### 核心模块说明

#### 前端模块 (`src/features/`)

1. **Skills 模块** (`src/features/skills/`)
   - **SkillsDashboard**：主仪表盘，展示 Skills 列表
   - **AddSkillModal**：添加 Skill 弹窗（本地/Git/快速安装）
   - **DeleteModal**：删除确认弹窗
   - **Hooks**：
     - `useSkillManagement`：Skill 管理逻辑
     - `useSkillContent`：Skill 内容加载和编辑
     - `useTranslation`：翻译功能

2. **Discovery 模块** (`src/features/discovery/`)
   - **DiscoveryModal**：发现市场弹窗
   - **Hooks**：
     - `useDiscovery`：发现数据加载和筛选

3. **Analytics 模块** (`src/features/analytics/`)
   - **AnalyticsDashboard**：数据分析仪表盘
   - 支持调用趋势、成功率、成本分析等可视化

4. **Tools 模块** (`src/features/tools/`)
   - **NewToolsModal**：新工具检测提示
   - **SharedDirModal**：共享目录冲突处理
   - **Hooks**：
     - `useToolManagement`：工具管理逻辑

5. **Settings 模块** (`src/features/settings/`)
   - **SettingsModal**：设置弹窗
   - 支持主题、语言、存储路径、缓存配置等

#### 后端模块 (`src-tauri/src/core/`)

1. **Tool Adapters** (`tool_adapters/`)
   - 定义各工具的适配器（路径、检测逻辑）
   - 支持 40+ AI 编码工具

2. **Sync Engine** (`sync_engine.rs`)
   - 混合同步策略（symlink/junction/copy）
   - 冲突检测和处理

3. **Installer** (`installer.rs`)
   - 本地 Skill 安装
   - Git Skill 安装和更新
   - 多 Skill 仓库候选选择

4. **Git Fetcher** (`git_fetcher.rs`)
   - Git 仓库拉取（优先系统 git，回退 libgit2）
   - URL 解析和验证

5. **Skill Store** (`skill_store.rs`)
   - SQLite 数据库操作
   - Skills 和 Targets 数据管理

6. **Analytics Store** (`analytics_store.rs`)
   - 分析数据存储
   - 调用追踪、趋势分析

7. **Discovery Module** (`discovery.rs`, `discovery_config.rs`, `discovery_parser.rs`, `discovery_remote.rs`)
   - Skill 发现和解析
   - 远程数据同步

8. **Onboarding** (`onboarding.rs`)
   - 扫描现有 Skills
   - 冲突检测和导入计划生成

9. **Content Hash** (`content_hash.rs`)
   - 目录指纹计算
   - 用于冲突检测和更新验证

10. **Temp Cleanup** (`temp_cleanup.rs`, `cache_cleanup.rs`)
    - 临时文件清理
    - Git 缓存管理

11. **Auto Update** (`auto_update.rs`)
    - 自动更新检测
    - 批量更新执行

12. **Chat Server** (`chat_server.rs`)
    - AI 对话服务器
    - Skill 优化功能

13. **Analytics Ingest** (`analytics_ingest.rs`)
    - 分析数据接收服务
    - MCP Server 集成

## 🚀 快速开始

### 环境要求

- **Node.js**：18+（推荐 20+）
- **Rust**：stable 版本
- **系统依赖**：根据 Tauri 官方文档安装对应平台的依赖

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run tauri:dev
```

### 构建生产版本

```bash
# 检查代码
npm run lint

# 构建前端
npm run build

# 构建 Tauri 应用
npm run tauri:build
```

### 平台特定构建

#### macOS
```bash
# DMG 格式
npm run tauri:build:mac:dmg

# 通用 DMG（支持 Apple Silicon 和 Intel）
npm run tauri:build:mac:universal:dmg
```

#### Windows
```bash
# MSI 安装包
npm run tauri:build:win:msi

# NSIS 安装程序
npm run tauri:build:win:exe

# 同时构建 MSI 和 NSIS
npm run tauri:build:win:all
```

#### Linux
```bash
# DEB 包
npm run tauri:build:linux:deb

# AppImage
npm run tauri:build:linux:appimage

# 同时构建 DEB 和 AppImage
npm run tauri:build:linux:all
```

## 📦 支持的 AI 编码工具

支持 40+ AI 编码工具，包括但不限于：

| 工具 | 技能目录 |
|------|----------|
| Cursor | `~/.cursor/skills` |
| Claude Code | `~/.claude/skills` |
| Codex | `~/.codex/skills` |
| Windsurf | `~/.codeium/windsurf/skills` |
| GitHub Copilot | `~/.copilot/skills` |
| Cline | `~/.cline/skills` |
| Continue | `~/.continue/skills` |
| OpenHands | `~/.openhands/skills` |
| Trae | `~/.trae/skills` |
| Qwen Code | `~/.qwen/skills` |
| ... | ... |

完整列表请参考 [`README.md`](README.md) 中的 "Supported AI Coding Tools" 章节。

## 🎨 功能详解

### Skill 列表与卡片

#### Skills Dashboard
- **位置**：`src/features/skills/components/SkillsDashboard.tsx`
- **功能**：
  - 展示所有托管的 Skills
  - 支持搜索、排序、分类筛选
  - 显示每个 Skill 的来源类型（本地/Git）、更新时间、同步状态
  - 提供批量更新、刷新功能

#### Skill 卡片
- **显示信息**：
  - Skill 名称和描述
  - 来源标签（本地文件夹/Git 仓库）
  - 更新时间（相对时间格式）
  - 工具激活状态（Pills）
  - GitHub 链接（如果是 Git Skill）
- **操作按钮**：
  - 更新按钮（Git Skills）
  - 删除按钮
  - 工具切换开关

### Skill 详情页

#### 路由
- **路径**：`/skill/:skillId`
- **组件**：`src/pages/SkillDetail.tsx`

#### 核心功能

1. **文件浏览器**
   - 显示 Skill 的完整文件结构
   - 支持展开/折叠目录
   - 点击文件加载内容

2. **多模式编辑器**
   - **编辑模式**：
     - 使用 EasyMDE 编辑器
     - 支持 Markdown 语法高亮
     - 实时保存更改
   - **预览模式**：
     - 使用 React Markdown 渲染
     - 支持代码、数学公式、Mermaid 图表
   - **分屏模式**：
     - 左侧编辑，右侧预览
     - 实时同步
   - **翻译模式**：
     - 一键翻译 Skill 内容
     - 支持多种翻译服务（Google、LibreTranslate）
     - 显示翻译进度和错误
   - **优化模式**：
     - 集成 AI 对话面板
     - 提供优化建议
     - 支持自定义工作目录

3. **工具栏**
   - 返回按钮
   - 保存按钮
   - 视图模式切换
   - 文件树显示切换
   - 目标语言选择（翻译模式）

### Skill 发现与安装

#### 发现市场
- **位置**：`src/features/discovery/components/DiscoveryModal.tsx`
- **功能**：
  - 浏览社区精选 Skills
  - 按分类筛选
  - 搜索 Skills
  - 一键安装
  - 显示已安装状态

#### 添加 Skill
- **位置**：`src/features/skills/components/AddSkillModal.tsx`
- **三种方式**：
  1. **快速安装**：
     - 推荐热门 Skills
     - 一键安装
     - 查看详情
  2. **本地导入**：
     - 选择本地文件夹
     - 可选自定义名称
     - 选择同步目标工具
  3. **Git 导入**：
     - 输入 Git 仓库 URL
     - 自动解析和克隆
     - 多 Skill 仓库候选选择
     - 选择同步目标工具

#### 多 Skill 仓库处理
- 自动检测仓库中的多个 Skills
- 解析 `SKILL.md` 获取名称和描述
- 提供候选选择界面
- 支持选择子目录安装

## 📊 MCP Server 集成

### 功能特性

- **track_skill_invoke**：追踪 Skill 调用（耗时、成功/失败、成本）
- **track_user_feedback**：记录用户反馈（👍/👎）
- **query_analytics**：查询分析数据（概览、趋势、热门 Skills）
- **export_analytics_data**：导出数据（JSON/CSV）
- **get_analytics_overview**：快速获取关键指标

### 配置步骤

1. **构建 MCP Server**：
```bash
cd mcp-analytics-server
npm install
npm run build
```

2. **配置 Claude Desktop**：
在 `claude_desktop_config.json` 中添加：
```json
{
  "mcpServers": {
    "skills-hub-analytics": {
      "command": "node",
      "args": ["/absolute/path/to/skills-hub/mcp-analytics-server/dist/index.js"],
      "env": {
        "ANALYTICS_ENDPOINT": "http://127.0.0.1:19823"
      }
    }
  }
}
```

3. **启动 Skills Hub**（会自动在 19823 端口启动接收服务）
4. **重启 Claude Desktop**

### 使用示例

向 Claude 提问：
- "追踪这个 API 调用：skill_id=weather, duration=1.5s, success=true"
- "显示过去 7 天的分析概览"
- "导出上个月的数据到 CSV"

详细示例请参考 [`mcp-analytics-server/USAGE_EXAMPLES.md`](mcp-analytics-server/USAGE_EXAMPLES.md)。

## 🔌 Analytics SDK 集成

### 功能特性

Skills Hub 提供了 **@skillshub/analytics** SDK，用于在 Skills 中集成分析能力：

- **零入侵追踪**：无需修改代码结构即可追踪函数调用
- **自动批处理**：事件缓冲并自动刷新到 Skills Hub
- **离线支持**：服务器不可用时本地缓存事件
- **丰富元数据**：追踪成本、调用者信息和自定义元数据
- **用户反馈**：内置用户反馈收集支持
- **类型安全**：完整的 TypeScript 支持和全面的类型定义
- **数据导出**：支持将追踪数据导出为 JSON 或 CSV 格式

### 安装

```bash
npm install @skillshub/analytics
```

### 快速开始

#### 基础用法

```typescript
import { SkillsHubTracker } from '@skillshub/analytics'

// 初始化追踪器
const tracker = new SkillsHubTracker({
  skillId: 'my-skill-id',
  endpoint: 'http://127.0.0.1:19823', // 可选，默认为 localhost:19823
  bufferSize: 100,                   // 可选，默认为 100
  flushIntervalMs: 5000,             // 可选，默认为 5000ms
})

// 追踪函数调用
const span = tracker.startInvoke({
  sessionId: 'session-123',
  inputHash: 'input-hash-abc',
  metadata: { userId: 'user-456' }
})

try {
  // 你的函数逻辑
  const result = await myFunction()
  span.success()
} catch (error) {
  span.fail(error)
}

// 完成后清理
await tracker.shutdown()
```

#### 零入侵函数包装

```typescript
import { SkillsHubTracker } from '@skillshub/analytics'

const tracker = new SkillsHubTracker({ skillId: 'my-skill-id' })

// 包装任何异步函数
const trackedFetch = tracker.wrap(fetchWeather, {
  sessionId: 'session-123',
  hashInput: (args) => JSON.stringify(args),  // 可选：自定义哈希函数
  extractMetadata: (args) => ({ city: args[0] }) // 可选：提取元数据
})

// 像使用原函数一样使用包装后的函数
const weather = await trackedFetch('Beijing')
// 事件会自动被追踪！
```

### 追踪成本和调用者信息

```typescript
const span = tracker.startInvoke({ sessionId: 'session-123' })

// 追踪成本信息
span.setCost({
  token_input: 100,
  token_output: 50,
  api_cost_usd: 0.001
})

// 追踪调用者信息
span.setCaller({
  agent_id: 'agent-123',
  workflow_id: 'workflow-456',
  tool_key: 'tool-789'
})

span.success()
```

### 用户反馈追踪

```typescript
// 追踪用户反馈（点赞/点踩）
tracker.feedback({
  sessionId: 'session-123',
  score: 1,  // 1 表示点赞，-1 表示点踩
  metadata: { source: 'ui-button' }
})
```

### 数据导出功能

SDK 支持将追踪的事件数据导出为 JSON 或 CSV 格式：

```typescript
// 导出为 JSON
const jsonResult = await tracker.exportEvents('json', './output/data.json')
console.log(`导出了 ${jsonResult.eventCount} 个事件到 ${jsonResult.filePath}`)

// 导出为 CSV
const csvResult = await tracker.exportEvents('csv', './output/data.csv')
console.log(`导出了 ${csvResult.eventCount} 个事件到 ${csvResult.filePath}`)
```

导出功能适用于以下场景：
- **自定义报告**：根据业务需求生成定制化的分析报告
- **数据集成**：将分析数据集成到现有的 BI 工具或数据仓库
- **趋势分析**：在 Excel 或其他数据工具中进行深入的趋势分析和可视化
- **审计跟踪**：保存历史追踪数据以进行审计和对比
- **团队共享**：与团队成员或利益相关者共享分析结果
- **离线分析**：在没有网络连接的情况下分析追踪数据

### 配置选项

```typescript
interface TrackerConfig {
  skillId: string              // Skills Hub 安装时分配的技能 ID
  endpoint?: string            // 数据接收服务器端点（默认：http://127.0.0.1:19823）
  bufferSize?: number          // 自动刷新前缓冲的最大事件数（默认：100）
  flushIntervalMs?: number     // 自动刷新间隔（毫秒）（默认：5000）
  fallbackPath?: string        // 离线事件存储目录（默认：~/.skillshub/analytics_buffer/）
  userId?: string              // 用户 ID — 如果不提供则自动生成
}
```

### 事件类型

SDK 追踪以下事件类型：

- **skill_invoke**：函数/技能调用，包含成功/失败状态
- **skill_feedback**：用户反馈（点赞/点踩）
- **skill_error**：错误事件，包含详细错误信息

### 最佳实践

1. **务必关闭**：应用程序退出时调用 `await tracker.shutdown()`
2. **使用有意义的会话 ID**：使用相同的会话 ID 分组相关操作
3. **添加相关元数据**：包含有助于调试和分析的上下文
4. **追踪成本**：通过附加成本信息监控 API 成本
5. **处理错误**：始终使用 try-catch 块和 span.fail()
6. **导出分析数据**：使用导出功能进行自定义分析和报告生成

### 服务器要求

SDK 向 HTTP 数据接收服务器发送事件，规格如下：

- **端点**：`POST /v1/events`
- **Content-Type**：`application/json`
- **请求体**：

```json
{
  "events": [
    {
      "event_type": "skill_invoke",
      "skill_id": "my-skill-id",
      "timestamp": "2026-02-16T00:00:00Z",
      "user_id": "user-123",
      "session_id": "session-456",
      "input_hash": "abc123",
      "success": true,
      "duration_ms": 150,
      "error": null,
      "feedback_score": null,
      "cost": null,
      "caller": null,
      "metadata": {}
    }
  ]
}
```

### 详细文档

更多详细信息和高级用法，请参考：
- [SDK README](sdk/analytics/README.md)
- [Analytics SDK 指南](docs/analytics-sdk-guide.md)

## 🔧 配置与设置

### 存储路径
- **默认位置**：`~/.skillshub`
- **自定义**：在设置中更改

### 缓存管理
- **Git 缓存清理**：自动清理超过配置天数的缓存
- **临时文件清理**：自动清理 24 小时前的临时文件

### 主题设置
- **浅色模式**：适合白天使用
- **深色模式**：适合夜间使用
- **系统模式**：跟随系统主题自动切换

### 翻译服务
- **Google 翻译**：需要 API Key
- **LibreTranslate**：支持自建服务
- **无翻译**：禁用翻译功能

## 🧪 测试

### Rust 测试
```bash
cd src-tauri
cargo test
```

### 代码检查
```bash
# ESLint
npm run lint

# Rust 格式化
npm run rust:fmt

# Rust Clippy
npm run rust:clippy

# Rust 测试
npm run rust:test

# 完整检查
npm run check
```

## 📝 开发指南

### 项目结构

```
skills-hub/
├── src/                          # 前端源码
│   ├── features/                 # 功能模块
│   │   ├── skills/              # Skill 管理
│   │   ├── discovery/           # 发现市场
│   │   ├── analytics/           # 数据分析
│   │   ├── tools/               # 工具管理
│   │   └── settings/            # 设置
│   ├── components/              # 公共组件
│   ├── hooks/                   # 自定义 Hooks
│   ├── shared/                  # 共享代码
│   ├── pages/                   # 页面
│   └── services/                # 服务
├── src-tauri/                   # 后端源码
│   ├── src/
│   │   ├── core/               # 核心模块
│   │   ├── commands/           # Tauri 命令
│   │   └── main.rs             # 入口
│   └── Cargo.toml
├── mcp-analytics-server/        # MCP Server
├── sdk/analytics/               # Analytics SDK
├── docs/                        # 文档
└── package.json
```

### 添加新功能

1. **前端功能**：
   - 在 `src/features/` 下创建新模块
   - 使用 Hooks 封装业务逻辑
   - 遵循现有的组件结构

2. **后端功能**：
   - 在 `src-tauri/src/core/` 下添加核心逻辑
   - 在 `src-tauri/src/commands/` 下暴露命令
   - 更新 `src-tauri/src/lib.rs` 注册命令

3. **数据库变更**：
   - 在 `src-tauri/src/core/skill_store.rs` 中添加表结构
   - 实现相应的 CRUD 操作

## ❓ 常见问题

### Skills 存储在哪里？
默认存储在 `~/.skillshub`（可在设置中自定义）。

### 为什么 Cursor 同步总是复制？
Cursor 目前不支持基于符号链接的技能目录，因此 Skills Hub 在同步到 Cursor 时强制使用目录复制。

### 为什么同步有时会回退到复制？
Skills Hub 优先使用符号链接/目录连接，但在某些系统（尤其是 Windows）上，符号链接可能受权限限制，此时会回退到目录复制。

### `TARGET_EXISTS|...` 是什么意思？
目标文件夹已存在，操作未覆盖（默认是非破坏性的）。删除现有文件夹或使用适当的覆盖流程重试。

### macOS Gatekeeper 提示怎么办？
如果看到 "已损坏" 或 "未验证的开发者"，运行：
```bash
xattr -cr "/Applications/Skills Hub.app"
```

## 🤝 贡献

欢迎贡献！请参考：
- [贡献指南](CONTRIBUTING.md)
- [行为准则](CODE_OF_CONDUCT.md)
- [安全政策](SECURITY.md)

## 📄 许可证

MIT License — 详见 [LICENSE](LICENSE)

## 🙏 致谢

感谢所有贡献者和开源项目的支持！

---

**Skills Hub** - 让 AI Agent Skills 管理变得简单高效 🚀
